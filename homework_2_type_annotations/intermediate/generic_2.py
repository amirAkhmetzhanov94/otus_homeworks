"""
TODO:

The function `add` accepts two arguments and returns a value,
they all have the same type. The type can only be str or int.
"""


def add[TypeConstraints: (
    str,
    int,
)](a: TypeConstraints, b: TypeConstraints) -> TypeConstraints:
    return b
